<%= form_for(@bookrack) do |f| %>
  <% if @bookrack.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bookrack.errors.count, "error") %> prohibited this bookrack from being saved:</h2>

      <ul>
      <% @bookrack.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
